---
phase: 01-build-pipeline-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - scripts/ci/check-dependency-drift.mjs
  - package.json
  - .github/workflows/nodejs.yml
autonomous: true
requirements:
  - BLD-03
must_haves:
  truths:
    - "CI fails when shared runtime dependencies drift in major version between package and demo"
    - "Drift check is runnable locally and in CI via the same repository script"
    - "Failure output names mismatched packages and remediation actions"
  artifacts:
    - path: "scripts/ci/check-dependency-drift.mjs"
      provides: "Major-version drift enforcement logic"
    - path: "package.json"
      provides: "Root script wiring for drift check in canonical pipeline"
  key_links:
    - from: "scripts/ci/check-dependency-drift.mjs"
      to: "components/astro/package.json"
      via: "dependency parsing"
      pattern: "@tsparticles/engine"
    - from: "scripts/ci/check-dependency-drift.mjs"
      to: "apps/astro/package.json"
      via: "dependency parsing"
      pattern: "dependencies"
    - from: "package.json#scripts.build:ci"
      to: "scripts/ci/check-dependency-drift.mjs"
      via: "pnpm run ci:check-dependency-drift"
      pattern: "ci:check-dependency-drift"
---

<objective>
Implement dependency major-version drift enforcement between `components/astro` and `apps/astro` and finalize Phase 1 canonical CI gate ordering.

Purpose: Satisfy BLD-03 and decisions D-13..D-16 while preserving full-fidelity behavior from user decisions.
Output: Versioned drift-check script with major-only policy, root/CI wiring, and remediation-focused diagnostics.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-pipeline-hardening/01-CONTEXT.md
@.planning/phases/01-build-pipeline-hardening/01-RESEARCH.md
@.planning/phases/01-build-pipeline-hardening/01-01-SUMMARY.md
@.planning/phases/01-build-pipeline-hardening/01-02-SUMMARY.md
@components/astro/package.json
@apps/astro/package.json
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement shared runtime dependency major-drift checker</name>
  <read_first>.planning/phases/01-build-pipeline-hardening/01-CONTEXT.md, components/astro/package.json, apps/astro/package.json</read_first>
  <files>scripts/ci/check-dependency-drift.mjs</files>
  <behavior>
    - Test 1: Script exits 0 when shared runtime dependencies have matching majors.
    - Test 2: Script exits non-zero and reports package-level mismatches when majors differ.
    - Test 3: Script ignores minor/patch differences when majors match.
  </behavior>
  <action>Implement D-13, D-14, and D-16 by creating `scripts/ci/check-dependency-drift.mjs` that loads dependency maps from `components/astro/package.json` and `apps/astro/package.json`, computes intersection of shared runtime dependencies, extracts semver majors, and fails only for major mismatches. Emit per-package mismatch table (component version vs app version) and remediation hints to align major ranges.</action>
  <acceptance_criteria>
    - Script reads both package manifests and compares shared runtime dependency majors.
    - Exit code is non-zero for major mismatches and zero otherwise.
    - Output includes dependency name, component range, app range, and required action.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/ci/check-dependency-drift.mjs</automated>
  </verify>
  <done>Major drift detection exists as versioned repository logic usable in local and CI flows.</done>
</task>

<task type="auto">
  <name>Task 2: Wire drift check into canonical build gate and CI execution</name>
  <read_first>package.json, .github/workflows/nodejs.yml, scripts/ci/check-dependency-drift.mjs</read_first>
  <files>package.json, .github/workflows/nodejs.yml</files>
  <action>Implement D-02 and D-15 by adding root script `ci:check-dependency-drift` and appending it as the final blocking step in `build:ci` after toolchain check, package build/typecheck, app build, and tarball validation. Ensure `.github/workflows/nodejs.yml` executes only `pnpm build:ci` as the canonical gate path (no alternate fallback command) so drift mismatches always fail CI with script diagnostics.</action>
  <acceptance_criteria>
    - `package.json` contains `ci:check-dependency-drift` and `build:ci` ordered pipeline ending with drift check.
    - `.github/workflows/nodejs.yml` runs `pnpm build:ci` as the single build gate command.
    - CI fails when `node scripts/ci/check-dependency-drift.mjs` fails.
  </acceptance_criteria>
  <verify>
    <automated>pnpm build:ci</automated>
  </verify>
  <done>Dependency major drift is enforced as a hard CI failure in the canonical root pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace manifests → CI pass/fail | Incorrect dependency metadata can hide incompatibility risk |
| local execution → CI parity | Different local/CI checks can let drift slip through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | T | scripts/ci/check-dependency-drift.mjs | mitigate | Parse both manifests and enforce major-only policy on shared runtime dependencies with hard fail. |
| T-01-08 | D | package/runtime compatibility | mitigate | Block CI on major drift to prevent runtime breakage from incompatible engine packages. |
| T-01-09 | R | root build gate | mitigate | Route both local and CI through `pnpm build:ci` single entrypoint and deterministic ordering. |
</threat_model>

<verification>
- `node scripts/ci/check-dependency-drift.mjs` reports current drift state and exits with policy-appropriate code.
- `pnpm build:ci` runs complete ordered gate including drift check.
</verification>

<success_criteria>
- Shared runtime dependency major drift is hard-failed in CI and local gate.
- Drift diagnostics include mismatch details and remediation guidance.
- Phase 1 gate order matches D-02 contract end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-pipeline-hardening/01-03-SUMMARY.md`
</output>
