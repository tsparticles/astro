---
phase: 01-build-pipeline-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - scripts/ci/validate-tarball.mjs
  - scripts/ci/tarball-expected-manifest.json
  - package.json
  - components/astro/package.json
autonomous: true
requirements:
  - BLD-02
must_haves:
  truths:
    - "Maintainer can validate packed tarball exports/files/types before publish"
    - "CI fails on any unapproved tarball content drift"
    - "Validation emits machine-readable result and concise human summary"
  artifacts:
    - path: "scripts/ci/validate-tarball.mjs"
      provides: "Strict tarball validation logic for exports/files/types"
    - path: "scripts/ci/tarball-expected-manifest.json"
      provides: "Committed expected tarball file snapshot"
    - path: "components/astro/package.json"
      provides: "Allowlist-aligned publish file contract"
  key_links:
    - from: "package.json#scripts.build:ci"
      to: "scripts/ci/validate-tarball.mjs"
      via: "pnpm run ci:validate-tarball"
      pattern: "ci:validate-tarball"
    - from: "scripts/ci/validate-tarball.mjs"
      to: "scripts/ci/tarball-expected-manifest.json"
      via: "snapshot compare"
      pattern: "expected-manifest"
---

<objective>
Add strict tarball contract validation so publish artifacts are verified for exports/files/types and content drift is blocked in the canonical build gate.

Purpose: Satisfy BLD-02 and decisions D-08..D-12 without reducing strictness.
Output: Tarball validator script, committed expected manifest snapshot, and `build:ci` wiring for blocking enforcement.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-pipeline-hardening/01-CONTEXT.md
@.planning/phases/01-build-pipeline-hardening/01-RESEARCH.md
@.planning/phases/01-build-pipeline-hardening/01-01-SUMMARY.md
@components/astro/package.json
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement strict tarball exports/files/types validator</name>
  <read_first>.planning/phases/01-build-pipeline-hardening/01-CONTEXT.md, components/astro/package.json, package.json</read_first>
  <files>scripts/ci/validate-tarball.mjs, package.json, components/astro/package.json</files>
  <behavior>
    - Test 1: Validator passes only when all declared export targets and type files exist in packed tarball.
    - Test 2: Validator fails if tarball contains unexpected files outside allowlist policy.
    - Test 3: Validator writes machine-readable JSON result and human summary to stdout.
  </behavior>
  <action>Implement D-08, D-09, D-10, and D-11 by creating `scripts/ci/validate-tarball.mjs` that runs `pnpm pack --json` in `components/astro`, inspects tarball contents, verifies all `exports` targets plus `types` are present, enforces required runtime/docs/license files, and rejects non-allowlisted extras. Wire `ci:validate-tarball` into root `build:ci` as a blocking step after package/app builds. Ensure output includes a JSON report file under `.planning/tmp/` (or equivalent deterministic path) and concise pass/fail summary lines.</action>
  <acceptance_criteria>
    - `scripts/ci/validate-tarball.mjs` exists and reads packed file list via `pnpm pack --json`.
    - Root `build:ci` includes `pnpm run ci:validate-tarball` after build steps.
    - Validator emits both machine-readable JSON and human-readable summary.
    - Validator exits non-zero when a required export/types file is missing.
  </acceptance_criteria>
  <verify>
    <automated>pnpm run ci:validate-tarball</automated>
  </verify>
  <done>Tarball validation is strict, blocking, and informative for both machines and maintainers.</done>
</task>

<task type="auto">
  <name>Task 2: Add committed expected manifest snapshot and drift enforcement</name>
  <read_first>scripts/ci/validate-tarball.mjs, components/astro/package.json</read_first>
  <files>scripts/ci/tarball-expected-manifest.json, scripts/ci/validate-tarball.mjs</files>
  <action>Implement D-12 by generating and committing `scripts/ci/tarball-expected-manifest.json` from the validated tarball file list, then extending validator logic to compare current packed contents against snapshot and fail on unapproved drift. Include remediation guidance that tells maintainers to intentionally regenerate the snapshot only when package surface changes are deliberate.</action>
  <acceptance_criteria>
    - `scripts/ci/tarball-expected-manifest.json` exists and lists expected files deterministically.
    - Validator compares current tarball list to snapshot and fails on additions/removals.
    - Failure output names drifted files and instructs how to approve intentional drift.
  </acceptance_criteria>
  <verify>
    <automated>pnpm run ci:validate-tarball</automated>
  </verify>
  <done>Tarball content drift is blocked unless expected manifest is intentionally updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package build output → publish tarball | Incorrect or malicious file inclusion can escape to consumers |
| validator result → CI gate | Weak validation could allow compromised package artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | scripts/ci/validate-tarball.mjs | mitigate | Enforce allowlist + export/types presence checks and fail on unexpected files. |
| T-01-05 | I | tarball publish surface | mitigate | Block inclusion of unintended files by snapshot and explicit required-file policy. |
| T-01-06 | R | validation reporting | mitigate | Emit machine-readable report + concise summary to preserve evidence in CI logs/artifacts. |
</threat_model>

<verification>
- `pnpm run ci:validate-tarball` passes with current manifest and fails when tarball contract is violated.
- `pnpm build:ci` includes tarball validation step in ordered sequence.
</verification>

<success_criteria>
- Tarball checks are strict, blocking, and part of canonical `build:ci`.
- Export paths, file allowlist, and types are validated from the packed artifact.
- Expected manifest snapshot is committed and enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-pipeline-hardening/01-02-SUMMARY.md`
</output>
